#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Alphabet soup
*
* Return: Always 0 (Success)
*/
int main(void)
{
int n = 0;
for (n = 0 ; n <= 9 ; n++)
{
printf("%d", n);
}
putchar('\n');
return (0);
}
