#include "holberton.h"
/**
 * _strcat - print a string
 * @dest: The caracter to print
 * @src: string for concatenates
 *Return: Always 0.
 */
char *_strcat(char *dest, char *src)
  int main(void)
{
  char s1[98] = "Hello ";
  char s2[] = "World!\n";
  char *p;

  printf("%s\n", s1);
  printf("%s", s2);
  p = _strcat(s1, s2);
  printf("%s", s1);
  printf("%s", s2);
  printf("%s", p);
  return (0);
}{
int i;
i = 0;
while (str[i] != '\0')
{
_putchar(str[i]);
i++;
}
_putchar('\n');
}
